<h1>Questions</h1>

<ul class="question-list">
    <% @questions.each do |question| %>
        <li>
            <%= link_to(question.title, question_path(question)) %> <br>
        </li>
    <% end %>
</ul>